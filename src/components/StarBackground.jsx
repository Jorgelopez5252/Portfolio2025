import {useState} from "react";

//id, size, y,x opacity and animation duration


export const StarBackground = () => { 
    const [stars, setStars] = useState([])

    const generateStars =() => {
        const numberOfStar = Math.floor(window.innerWidth * window.innerHeight / 10000)
    }
    return <div></div> 
};

// 50:09